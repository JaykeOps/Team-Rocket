<Window x:Class="FootballManager.Admin.View.SeriesGameProtocolView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManager.Admin.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Height="700" Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        WindowStartupLocation="CenterScreen"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=SeriesGameProtocolViewModel}">
    <Grid Margin="5" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="0,0,0,40">
            <Grid.Resources>
                <Style BasedOn="{StaticResource GameProtocolCustomLabelStyle}" TargetType="{x:Type Label}" />
                <Style BasedOn="{StaticResource GameProtocolMainCustomTextBoxStyle}" TargetType="{x:Type TextBox}" />
                <Style BasedOn="{StaticResource GameProtocolFlatButton}" TargetType="{x:Type Button}" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Home Team" HorizontalAlignment="Center" FontSize="30"/>
                <Label Grid.Row="0" Grid.Column="1" Content="   -   " HorizontalAlignment="Center" FontSize="30"/>
                <Label Grid.Row="0" Grid.Column="2" Content="Away Team" HorizontalAlignment="Center" FontSize="30"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="0" HorizontalAlignment="Center" FontSize="30"/>
                <Label Grid.Row="1" Grid.Column="1" Content="   -   " HorizontalAlignment="Center" FontSize="30"/>
                <Label Grid.Row="1" Grid.Column="2" Content="0" HorizontalAlignment="Center" FontSize="30"/>

            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Grid.Resources>
                    <Style BasedOn="{StaticResource GameProtocolCustomLabelStyle}" TargetType="{x:Type Label}" />
                    <Style BasedOn="{StaticResource GameProtocolMainCustomTextBoxStyle}" TargetType="{x:Type TextBox}" />
                    <Style BasedOn="{StaticResource GameProtocolFlatButton}" TargetType="{x:Type Button}" />
                </Grid.Resources>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="goalButton" Grid.Row="0" Grid.Column="1" Content="Goal" IsEnabled="{Binding GoalMatchMinuteValid}" />
                <Button x:Name="assistButton" Grid.Row="0" Grid.Column="2" Content="Assist" IsEnabled="{Binding AssistMatchMinuteValid}" />
                <Button x:Name="penaltyButton" Grid.Row="0" Grid.Column="3" Content="Penalty" IsEnabled="{Binding PenaltyMatchMinuteValid}" />
                <Button x:Name="yellowCardButton" Grid.Row="0" Grid.Column="4" Content="Yellow card" IsEnabled="{Binding YellowCardMatchMinuteValid}" />
                <Button x:Name="redCardButton" Grid.Row="0" Grid.Column="5" Content="Red card" IsEnabled="{Binding RedCardMatchMinuteValid}" />

                <Label Grid.Row="1" Grid.Column="0" Content="Match minute: "/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DataContext.GoalMatchMinute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True, RelativeSource={RelativeSource AncestorType={x:Type local:SeriesGameProtocolView}}}">
                    <Validation.ErrorTemplate>
                        <ControlTemplate>
                            <StackPanel>
                                <AdornedElementPlaceholder />
                                <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" />
                            </StackPanel>
                        </ControlTemplate>
                    </Validation.ErrorTemplate>
                </TextBox>
                <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding DataContext.AssistMatchMinute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True, RelativeSource={RelativeSource AncestorType={x:Type local:SeriesGameProtocolView}}}">
                    <Validation.ErrorTemplate>
                        <ControlTemplate>
                            <StackPanel>
                                <AdornedElementPlaceholder />
                                <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" />
                            </StackPanel>
                        </ControlTemplate>
                    </Validation.ErrorTemplate>
                </TextBox>
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding DataContext.PenaltyMatchMinute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True, RelativeSource={RelativeSource AncestorType={x:Type local:SeriesGameProtocolView}}}">
                    <Validation.ErrorTemplate>
                        <ControlTemplate>
                            <StackPanel>
                                <AdornedElementPlaceholder />
                                <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" />
                            </StackPanel>
                        </ControlTemplate>
                    </Validation.ErrorTemplate>
                </TextBox>
                <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding DataContext.YellowCardMatchMinute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True, RelativeSource={RelativeSource AncestorType={x:Type local:SeriesGameProtocolView}}}">
                    <Validation.ErrorTemplate>
                        <ControlTemplate>
                            <StackPanel>
                                <AdornedElementPlaceholder />
                                <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" />
                            </StackPanel>
                        </ControlTemplate>
                    </Validation.ErrorTemplate>
                </TextBox>
                <TextBox Grid.Row="1" Grid.Column="5" Text="{Binding DataContext.RedCardMatchMinute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, 
                            ValidatesOnDataErrors=True, RelativeSource={RelativeSource AncestorType={x:Type local:SeriesGameProtocolView}}}">
                    <Validation.ErrorTemplate>
                        <ControlTemplate>
                            <StackPanel>
                                <AdornedElementPlaceholder />
                                <TextBlock Text="{Binding [0].ErrorContent}" Foreground="Red" />
                            </StackPanel>
                        </ControlTemplate>
                    </Validation.ErrorTemplate>
                </TextBox>

            </Grid>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="0" VerticalAlignment="Bottom">
            <Grid.Resources>
                <Style BasedOn="{StaticResource GameProtocolCustomLabelStyle}" TargetType="{x:Type Label}" />
                <Style BasedOn="{StaticResource GameProtocolCustomTextBoxStyle}" TargetType="{x:Type TextBox}" />
                <Style BasedOn="{StaticResource GameProtocolListBoxStyle}" TargetType="{x:Type ListBox}" />
                <Style BasedOn="{StaticResource GameListFlatButton}" TargetType="{x:Type Button}" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Row="0" Grid.Column="0" Margin="10,0,10,0">
                <Label Content="Home team" Margin="10,0,10,0" HorizontalAlignment="Left"/>
                <Button Content="+" Margin="10,5,10,5" HorizontalAlignment="Right"/>
            </DockPanel>

            <DockPanel Grid.Row="0" Grid.Column="1" Margin="10,0,10,0">
                <Label Content="Active players" Margin="10,0,10,0" HorizontalAlignment="Left"/>
                <Button Content="-" Margin="10,5,10,5" HorizontalAlignment="Right"/>
            </DockPanel>

            <ListBox Grid.Row="1" Grid.Column="0" Width="150" Height="400" Margin="2,0,2,0">
                <ListBoxItem>Spelare 1</ListBoxItem>
                <ListBoxItem>Spelare 2</ListBoxItem>
                <ListBoxItem>Spelare 3</ListBoxItem>
            </ListBox>
            <ListBox Grid.Row="1" Grid.Column="1" Width="150" Height="400" Margin="2,0,2,0">
                <ListBoxItem>Spelare 1</ListBoxItem>
                <ListBoxItem>Spelare 2</ListBoxItem>
                <ListBoxItem>Spelare 3</ListBoxItem>
            </ListBox>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="2,0,2,0">
            <Grid.Resources>
                <Style BasedOn="{StaticResource GameProtocolCustomLabelStyle}" TargetType="{x:Type Label}" />
                <Style BasedOn="{StaticResource GameProtocolCustomTextBoxStyle}" TargetType="{x:Type TextBox}" />
                <Style BasedOn="{StaticResource GameProtocolListBoxStyle}" TargetType="{x:Type ListBox}" />
                <Style BasedOn="{StaticResource GameListFlatButton}" TargetType="{x:Type Button}" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Row="0" Grid.Column="0" Margin="10,0,10,0">
                <Label Content="Events" Margin="10,0,10,0" HorizontalAlignment="Left"/>
                <Button Content="-" Margin="10,5,10,5" HorizontalAlignment="Right"/>
            </DockPanel>

            <ListBox Grid.Row="1" Grid.Column="0" Width="230" Height="400">
                <ListBoxItem>Goal, Min, Player, Team</ListBoxItem>
                <ListBoxItem>Goal, Min, Player, Team</ListBoxItem>
                <ListBoxItem>Goal, Min, Player, Team</ListBoxItem>
            </ListBox>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="2" VerticalAlignment="Bottom">
            <Grid.Resources>
                <Style BasedOn="{StaticResource GameProtocolCustomLabelStyle}" TargetType="{x:Type Label}" />
                <Style BasedOn="{StaticResource GameProtocolCustomTextBoxStyle}" TargetType="{x:Type TextBox}" />
                <Style BasedOn="{StaticResource GameProtocolListBoxStyle}" TargetType="{x:Type ListBox}" />
                <Style BasedOn="{StaticResource GameListFlatButton}" TargetType="{x:Type Button}" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Row="0" Grid.Column="0" Margin="10,0,10,0">
                <Label Content="Active players" Margin="10,0,10,0" HorizontalAlignment="Left"/>
                <Button Content="-" Margin="10,5,10,5" HorizontalAlignment="Right"/>
            </DockPanel>

            <DockPanel Grid.Row="0" Grid.Column="1" Margin="10,0,10,0">
                <Label Content="Away team" Margin="10,0,10,0" HorizontalAlignment="Left"/>
                <Button Content="+" Margin="10,5,10,5" HorizontalAlignment="Right"/>
            </DockPanel>

            <ListBox Grid.Row="1" Grid.Column="0" Width="150" Height="400" Margin="2,0,2,0">
                <ListBoxItem>Spelare 1</ListBoxItem>
                <ListBoxItem>Spelare 2</ListBoxItem>
                <ListBoxItem>Spelare 3</ListBoxItem>
            </ListBox>

            <ListBox Grid.Row="1" Grid.Column="1" Width="150" Height="400" Margin="2,0,2,0">
                <ListBoxItem>Spelare 1</ListBoxItem>
                <ListBoxItem>Spelare 2</ListBoxItem>
                <ListBoxItem>Spelare 3</ListBoxItem>
            </ListBox>
        </Grid>

        <Button Grid.Row="2" Grid.ColumnSpan="3" Content="Save Match Protocol" Margin="10"  HorizontalAlignment="Right"/>
        
    </Grid>
</Window>
